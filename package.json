{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lerna": "lerna",
    "bootstrap": "lerna bootstrap",
    "dev:server": "yarn lerna run serve --scope=@hango/ui-server --stream",
    "start": "yarn lerna run start --scope=@hango/ui-server --stream",
    "dev:ui": "yarn lerna run serve --scope=@hango/ui --stream",
    "build": "yarn lerna run build --scope=@hango/ui --stream"
  },
  "devDependencies": {
    "eslint-config-2o3t": "^2.0.2",
    "eslint-plugin-jest": "^23.20.0",
    "jest": "^26.6.3",
    "lerna": "^4.0.0"
  },
  "dependencies": {
  }
}
